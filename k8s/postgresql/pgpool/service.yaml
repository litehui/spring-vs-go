apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: database
  labels:
    app: pgpool
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    name: pgpool
  selector:
    app: pgpool
