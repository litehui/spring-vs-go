apiVersion: v1
kind: Service
metadata:
  name: redis-master
  namespace: cache
  labels:
    app: redis
    role: master
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  selector:
    app: redis
    role: master
