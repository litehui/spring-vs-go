apiVersion: v1
kind: Service
metadata:
  name: redis-replica
  namespace: cache
  labels:
    app: redis
    role: replica
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  selector:
    app: redis
    role: replica
